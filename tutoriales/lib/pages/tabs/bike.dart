import"package:flutter/material.dart";


class Bike extends StatelessWidget {
const Bike({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return const Center(
            child: Text('Bike desde archivo separado'),
          );
  }
}

